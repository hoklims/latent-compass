Reprends l’implémentation de Latent Compass jusqu’au résultat demandé : choisir les observations utiles à une décision sous budget, avec un routage gouverné, puis permettre le retrait des index uniquement lorsque les preuves le permettent. Poursuis le travail effectif ; ne t’arrête pas à un nouveau plan.

## Dépôt et point de départ

- Worktree à utiliser : `E:\latent-compass-hok798`.
- Branche : `codex/hok-798-active-diagnosis`, base `6ec94e86f5b6c429fd264708f28d5105b19fbafa`.
- PR existante, ouverte en brouillon : https://github.com/hoklims/latent-compass/pull/4
- Le dernier commit de code est celui de `git log -1` sur cette branche ; vérifie l’état réel et la CI de ce SHA avant d’écrire. Ne réinitialise aucun checkout et conserve les modifications d’autrui. Les anciens auteurs délégués sont arrêtés.

## À lire d’abord

1. Les instructions applicables au worktree, puis `HANDOFF.md`, `GOVERNANCE.md` et `CONTRIBUTING.md`.
2. `docs/adr/0011-experimental-active-diagnosis.md` et son amendement, `docs/active-diagnosis.md`, `docs/active-diagnosis-scope.md`, `docs/host-observations.md`, `docs/source-session.md`, `docs/source-observations.md`, `docs/lab-operations.md`.
3. La PR #4 et les issues Linear HOK-798 à HOK-806, descriptions et dépendances comprises.
4. Sur cette machine, les preuves sont hors dépôt, sous le profil de l’opérateur. Session Codex du 2026-09-18 : `Documents/Codex/2026-09-18/fai/work/` (commit `9742953`). Session Claude du 2026-09-19 : `Documents/Claude/2026-09-19/latent-compass-hok798/work/` — epoch, sortie du classifieur, témoins rouges/verts liés aux objets Git, les rapports d’audit indépendants et les lanceurs de mutants. Chaque preuve porte sur le SHA qu’elle nomme, pas sur tes futures modifications.

## État réel

Le socle expérimental `latent_compass.lab` est implémenté : modèle fini explicite, calcul rationnel exact sous budget/horizon, abstention et preuves obligatoires, rejeu complet, mémoire de justifications invalidable, lectures confinées, boucle source→observation, contrôle d’avis/budgets, comparaison descriptive et migration à blanc. Il n’est branché sur aucun hôte actif.

Livré depuis la reprise du 2026-09-19 :

- **HOK-799** — périmètre figé dans `docs/active-diagnosis-scope.md` : un poste personnel, Claude Code et Codex, ce dépôt et ses worktrees comme seul périmètre `PERSONAL_LAB`. Inventaire en lecture seule de 25 actifs, scellé en deux révisions sous `evidence/` : la première reste telle que committée, défauts compris ; la `v1.1.0` la corrige sans l’éditer. Seuls deux actifs Graphify du pilote sortent de `KEEP`, en `DISABLE_LATER` avec leurs sept gates manquants. Six scénarios d’acceptation exécutables, neuf inconnues avec responsable. **Décision U1 du propriétaire, 2026-09-19** : un second dépôt personnel, avec CCC provisionné, rejoint le pilote. Il voyage ici sous l’alias `pilot-second-repository` ; la table alias → chemin est tenue par l’opérateur, hors dépôt. Ses artefacts d’index, lus le jour même en lecture seule, sont scellés dans la révision `v1.2.0` — la `v1.1.0` actif pour actif, plus cinq — à côté des deux autres, inchangées. Trois couples index/usage y deviennent candidats (son index CCC et ses deux artefacts Graphify) : `DISABLE_LATER`, sept gates manquants, jamais une permission. Sa couche Semctx et son cache d’outil symbolique restent gardés, par leur seule classe. Ce dépôt ne s’observe qu’en lecture seule : n’y écris rien, n’y lance aucun provider.
- **HOK-800** — `latent_compass.lab.host_observations` : une enveloppe unique pour lecture de fichier, recherche littérale, diff Git, navigation symbolique et contrôle ciblé ; sept statuts, limites obligatoires scellées, mode source seule comme témoin, provider retiré refusé sans repli. Le laboratoire n’exécute rien : l’hôte exécute, le laboratoire admet puis recoupe contre le snapshot.
- **HOK-801/802** — `latent_compass.lab.host_session` : pont observation vérifiée → issue du modèle ou `UNKNOWN`. L’identité et la version de l’outil sont scellées dans l’épisode ; un `UNKNOWN` laisse l’état intact et la sonde non consommée.
- **HOK-802, contrat `1.1.0`** — décision du propriétaire du 2026-09-19 : `propose_excluding` planifie autour des sondes que l’hôte déclare introuvables, à chaque nœud de la recherche, et rend un `ConstrainedPlanReport` `1.1.0` **à côté** du `PlanReport` `1.0.0`, dont la sortie est inchangée à l’octet. Aucun des deux lecteurs n’accepte la version de l’autre. CLI : `propose --unobtainable-probe`. Un identifiant inconnu ou répété est refusé avant toute recherche ; l’exclusion reste une déclaration de l’hôte, inscrite sur le rapport. Le banc s’en sert : outil symbolique absent, il exécute le contrôle (7/2 contre 15/4) au lieu de s’arrêter. Valeurs confrontées à une énumération indépendante ; amendement de l’ADR 0011 épinglé.
- **HOK-803** — `examples/lab_host_bench.py` : boucle complète dans un dépôt Git jetable, avec de vrais outils (git, `ast`, processus de contrôle). Exerce outil absent, timeout interrompu, langage non supporté, conseiller absent, kill switch, capacité retirée, avis périmé ou conflictuel, preuve Semctx manquante, avis ignoré, budget épuisé, provider retiré et séparation des deux familles d’agents. `run_delegation` confie le même diagnostic à des épisodes enfants qui réservent sur **un seul** pool : enfant affamé, enfant planifiant sur un reste périmé refusé par le pool, délégation annulée, checkout jamais partagé. Ce sont des épisodes du banc exécutés l’un après l’autre, pas des sous-agents réels. Avec `--symbol-tool pyright-langserver`, la question symbolique est posée à un vrai serveur de langage trouvé sur le `PATH` de l’hôte, par le protocole LSP : lancé pour la seule question, il reçoit les octets mêmes que la preuve digère, et il est tué s’il dépasse l’échéance. Ce n’est pas une dépendance du paquet. L’enregistrement déclare l’index que ce serveur garde en mémoire (`internal_index_used: true`) et ne prétend pas savoir s’il a utilisé un cache (`cache_used: null`). Ces chemins ne tournent que là où le serveur est installé — la CI ne l’a pas, leurs tests y sont ignorés avec cette raison ; serveur absent (`TOOL_ABSENT`) et binaire qui n’est pas un serveur de langage (`FAILED`) tournent partout.
- **HOK-804, squelette de préenregistrement** — `docs/active-diagnosis-preregistration.md` : ce que le propriétaire doit trancher avant le premier essai, écrit contre le contrat `lab.evaluation` `1.0.0` déjà livré. Onze décisions ont un champ du protocole scellé ; seize n’en ont aucun (snapshots et état des index, strates, exclusions, définition et jugement du succès, conversion des coûts, règle des comptes vers un verdict, puissance ou étude descriptive bornée, relances, mémoire, plan du bras contrôleur, hôtes et isolation, plafond de dépense, réutilisation d’une collecte, ancre d’antériorité, publication). Cinq couples candidats sur deux pilotes qui ne peuvent pas partager un protocole font de « un protocole ou plusieurs » une décision à part. **Première décision prise par le propriétaire le 2026-09-19, sur proposition de l’agent : groupé d’abord — un protocole par pilote, tous les couples candidats retirés d’un coup — puis couple par couple seulement là où le résultat groupé montre une régression.** Elle crée une obligation, donc une ligne de plus : la règle du second temps (ce qui compte comme régression, quels protocoles par couple tournent alors), à écrire avant le premier temps. **Quinze autres décisions ont suivi le même jour, en une séance** : le propriétaire a demandé la voie la plus efficiente ; l’agent a cessé de demander ligne à ligne, a proposé pour chaque ligne l’option la plus sévère pour son propre candidat, en langage simple avec une conséquence dite, une question fermée à la fois ; le propriétaire a répondu d’un mot. Le document le dit, et dit que c’est une appropriation plus faible qu’une lecture ligne à ligne ; rien n’est gelé, il peut tout rouvrir avant le gel. Pris : petite série descriptive bornée d’abord (elle ne peut jamais conclure `RETIRABLE_EN_PILOTE` ; sa variance dimensionne un protocole puissant enregistré à part — donc aucun retrait ne sortira de la première expérience) ; sessions réelles sur copies isolées ; aucune exclusion après gel ; nombre de paires fixé, une relance pour une panne avant la première action de l’agent, `MISSING` sinon ; mémoire de justification éteinte dans le bras contrôleur ; aucune réutilisation d’une collecte antérieure ; ancre = commit relu poussé avant tout essai + son run CI ; publication quel que soit le résultat ; succès jugé par contrôle mécanique quand il existe, sinon par un agent-juge à l’aveugle d’une autre famille que le coureur, le propriétaire auditant un essai sur cinq tiré avant la levée de l’aveugle (les deux familles ont co-écrit le candidat : aucun agent-juge n’est indépendant par sa famille, le document nomme les gardes qui restent) ; tâches = vrais changements passés des deux pilotes, tirés par une règle écrite, choisis à la main par personne, sur les six strates ; index reconstruits sur l’état de la copie (un index « tel quel » a été construit après le changement et contient la réponse) et coût d’entretien affiché à côté des essais ; une seule famille d’agents pour la petite série, Codex ; répétition générale hors protocole — six sessions réelles de quinze minutes au plus sur une copie jetable, durée et coût seulement, jamais une issue, payées par une clé dédiée sous une limite bloquante de 25 USD. Trois lignes décidées à moitié sont scindées ; « où geler les décisions sans champ » devient une ligne. Le document porte 34 décisions : 16 prises, 18 `OPEN` ; l’agent n’en a rempli aucune de son chef et rien n’a été mesuré. En préparant cette décision, quatre coûts d’option écrits par l’agent se sont révélés faux ou incomplets et sont corrigés : sans régression, un verdict lié à la configuration groupée se défend ; le bras témoin est rejoué pour chaque protocole, et deux index qui se couvrent paraissent chacun retirable seul ; le déclencheur du plan en deux temps avait un second sens, celui que le propriétaire a pris ; deux des cinq couples sont des caches sans consommateur déclaré côté agent. Cinq tests tiennent le document : chaque champ du protocole, chaque bras, chaque verdict et chaque couple candidat du rapport committé y sont nommés ; chaque décision est `OPEN` ou identique, mot pour mot, à un enregistrement épinglé dans le test, qui doit porter une valeur, une date et le propriétaire ; la ligne de statut compte ce que le document enregistre vraiment ; l’affirmation sur les caches est confrontée à l’inventaire committé.

- **HOK-804, lanceur de la répétition générale** — `examples/lab_rehearsal_launcher.py`, état au commit `f7a38ac` : le seul script du dépôt qui contacte un vrai agent. Côté hôte, donc hors du laboratoire, qui ne lance toujours rien ; ce n’est pas un lanceur d’essais et il ne produit aucun `LabTrial`. **Quatre relectures de sécurité indépendantes ont précédé toute exécution réelle, chacune par un agent neuf, et les quatre ont rendu « à corriger avant le premier lancement ».** La première a trouvé cinq défauts sérieux, tous sur des chemins d’erreur : session tuée puis attendue sans borne, interruption qui laissait la session payante vivante, suppression déclarée faite sans regarder, CLI résolu depuis le répertoire courant sous Windows, plafond « six sessions » lié au dossier de sortie. La deuxième a confirmé ces corrections et trouvé deux défauts sérieux : un run qui meurt sans dérouler la pile laissait la clé en clair sur le disque sans qu’aucun run ultérieur ne la cherche ; et le texte déchargeait ce que l’agent peut *lire*, jamais ce qu’il peut *écrire*. La troisième en a trouvé deux autres, **dont un introduit par la correction de la deuxième** : le balayage de la copie de clé d’un run mort supprimait *à travers* une jonction plantée sous ce nom — la connexion Codex habituelle du propriétaire, sur la plateforme où l’agent écrit partout — et la branche qui prouve qu’un kill a raté quelque chose ne disait rien et lançait la session payante suivante. La quatrième n’a trouvé aucun défaut sérieux et quatre défauts moindres qu’elle voulait fermés d’abord : sous POSIX, un terminal fermé ou un quit terminait le lanceur sans dérouler la pile, contre ce que la documentation promettait, et laissait une session payante sans rien pour la borner ; une suppression pouvait déclarer supprimé un nom qu’un lien avait pris entre-temps ; la politique des liens valait pour ce que le lanceur supprime, pas pour ce qu’il écrit ; aucun test ne tenait le drapeau de bac à sable d’une session. Le quatrième relecteur a ensuite relu deux fois ses propres constats. À `bf7bc62` : **« sûr à lancer une fois la clé créée »**, sous conditions (voir « Prochaine action unique »), avec trois défauts mineurs **que ces corrections avaient introduits** — un refus qui n’était pas celui du run pouvait être imprimé en son nom, sur un chemin que la production n’atteint pas ; la garde sur un shim batch de git ratait le seul argument que le lanceur construit lui-même ; un balayage interrompu était suivi d’une phrase annonçant une session tuée — et trois affirmations du texte que rien ne tenait : Python 3.12 « bruyant » par le seul hasard de l’ordre des appels ; une borne de prompt de 32 Kio très au-dessus de ce qu’un tube non lu accepte sous Windows, où l’écriture n’a pas d’échéance (mesuré sur le poste : 4 096 octets passent, 4 097 bloquent) ; « aucun drapeau ne change » le bac à sable, non montré pour ce que l’opérateur met dans `--codex-command`. À `f7a38ac`, qui les ferme : **même verdict, et pour la première fois un tour de corrections n’a rien introduit qui compte**. Trois remarques sont enregistrées comme connues et laissées, selon la règle d’arrêt annoncée au relecteur avant sa lecture — on ne retouche plus le lanceur que pour un constat qui bloque le lancement : sous Python 3.10 ou moins c’est un `ImportError` qui refuse, pas le message soigné ; le test qui écrit un prompt de taille maximale à un processus qui ne lit pas ne peut rougir que sous Windows — une CI verte sous Ubuntu ne dit rien de cette borne ; un test modifie `sys.version_info` le temps d’un import, puis le rend. La boucle de relectures s’arrête là. Chaque relecture a aussi trouvé des défauts moindres ; tout ce qu’un commit peut fermer est fermé. Ce que le lanceur garantit maintenant : il ne garde que durée, jetons, un statut de harnais et une *étiquette* de diagnostic d’un vocabulaire fixe — aucun texte de l’agent ni du CLI, sauf la version du CLI quand elle a la forme d’une version ; six sessions par dossier contenant le fichier de clé (registre et verrou à côté de la clé, noms fixes, le verrou nomme son processus), session comptée après sa copie et avant son processus, arrêt du run à la première session en échec ou dès que quelque chose survit au kill, et le code de sortie le dit (`4`) ; quinze minutes par session **tant que le lanceur vit**, plus une quarantaine de secondes pour tuer et drainer ; chaque session est lancée avec `--sandbox workspace-write` et `--ephemeral`, et un test tient les deux ; sans fichier de clé rien ne démarre, un fichier qui ne ressemble pas à une clé n’est pas envoyé, le CLI doit annoncer sa version avant de recevoir la clé (`--cli-version` l’épingle), la clé passe sur l’entrée standard vers un `CODEX_HOME` créé à côté du fichier de clé puis supprimé, dont la configuration épingle le stockage des identifiants dans un fichier, et le run s’arrête avant toute session si le login n’y a pas laissé ses identifiants ; chaque run cherche d’abord, sous le verrou, la copie de clé qu’un run mort aurait laissée, la supprime, le dit et refuse une fois ; **le lanceur ne supprime que de vrais dossiers qu’il a créés et vérifie que le dossier de clé en est un avant d’y écrire — un lien ou une jonction n’est jamais suivi ni retiré, il est nommé et le run refuse**. Ce sont des contrôles, pas des verrous : un nom échangé contre un lien entre un contrôle et l’écriture ou la suppression qu’il garde n’est pas vu, et ce qui ferait cela écrit déjà à côté du fichier de clé. **Ce qu’aucun commit ne ferme** : l’agent peut lire la clé sous laquelle il tourne — elle doit être **révoquée dès la répétition finie** ; sous `workspace-write` l’agent lit tout le système de fichiers par conception ; **sous Windows natif le CLI n’applique aucun bac à sable si sa configuration ne le demande pas, et le lanceur ne le demande pas** — l’agent peut donc lire et écrire tout ce que peut le compte qui lance, dossier Codex habituel, registre et verrou compris ; un lanceur tué net ne tue rien, la session finit seule (le correctif durable, un job object Windows, n’est pas construit) ; aucun test ne délivre un vrai signal : le test tient quels signaux sont pris en charge, et que les gestionnaires sont posés puis rendus. Il refuse de démarrer sous un Python antérieur à 3.12, refuse un prompt de plus de 4 096 octets (sous Windows l’écriture d’un prompt n’a pas d’échéance : c’est sa taille qui est bornée) et refuse d’emblée un git qui serait un shim batch. Cinquante-huit tests le pilotent avec un faux CLI — un vrai processus, aucune vraie session ; sous Windows, un test passe par un vrai shim `.cmd` et d’autres plantent une jonction sous le nom d’un dossier de clé. **La répétition elle-même n’a pas tourné.**

Vérification locale au commit `f7a38ac`, qui est désormais le dernier commit de code (les commits du lanceur ne touchent que `examples/`, ses tests et sa documentation ; `src/` n’a pas changé depuis `7c59b2b`) : Ruff, mypy, 1 422 tests réussis et 3 cas POSIX ignorés sous Windows, sur un poste où `pyright-langserver` est installé ; build et wheel validés hors checkout sur `bf3b73d` seulement, banc exécuté avec les deux outils symboliques. Les 917 tests de la base existent toujours. Vérifie la CI du SHA courant avant de t’en prévaloir.

**Quatre commits du lanceur ont été reconstruits avant d’être poussés.** Un test nommait deux fragments de l’emplacement du fichier de clé — emplacement qui ne doit vivre que dans Linear. Les commits locaux `e55d3e9`, `1d5623d`, `3c3264c` et `0535050`, jamais poussés, ont été reconstruits arbre par arbre en `f5ee088`, `1ef4886`, `f27083a` et `c7a296e` : mêmes messages, mêmes auteurs, mêmes dates, une seule ligne de `tests/test_lab_rehearsal_launcher.py` changée, et **le même blob pour `examples/lab_rehearsal_launcher.py`** — les relecteurs ont donc lu, octet pour octet, le lanceur que portent les commits publiés. Les messages de ces commits citent encore `e55d3e9` et `3c3264c` : ce sont les versions que les relecteurs ont lues, ils n’ont pas été réécrits. Les dossiers de preuve hors dépôt `proof-e55d3e9/` et `proof-3c3264c/` portent sur ces anciens SHA ; `proof-c7a296e/`, `proof-13cdf83/`, `proof-bf7bc62/` et `proof-f7a38ac/` portent sur l’historique publié.

Cinq revues indépendantes en lecture seule ont eu lieu, chacune par un agent neuf sans outil d’écriture ni d’exécution. Celle de HOK-799 a rendu `BOUNDARY_WEAK` ; ses treize constats sont traités. Celle du candidat agrégé a rendu **PROOF_WEAK** sans défaut fonctionnel établi : ses constats corrigeables dans le dépôt sont traités, et un témoin rouge/vert lié aux objets Git existe pour chacun des fichiers de test modifiés. Le lot U1 a été relu trois fois, à `5684870`, `2b73490` puis `c9fdccc` : **PROOF_WEAK** à chaque fois, sans bloquant, sans fuite et sans test affaibli. Chaque trou corrigeable a d’abord été reproduit — son mutant passait au vert sur le commit relu — puis fermé : vocabulaires `role` et `asset_id` épinglés, document de périmètre et `evidence/README.md` épinglés en entier, scan étendu aux documents et aux formes d’adresse, références de preuve affirmées vides, et fournisseur, rôle, classe et consommateurs épinglés actif par actif pour les cinq ajouts. `7a72403`, `d0733df`, `880fbe7` et `d816c95` n’ont pas été relus par un auditeur de preuve. Le lanceur, lui, a été relu quatre fois avant toute exécution, puis deux fois encore par le quatrième relecteur sur ses propres constats — par des relecteurs de **sécurité**, pas de preuve, tous consultatifs : aucun ne vaut reçu. Dernier verdict, à `f7a38ac` : « sûr à lancer une fois la clé créée », sous les conditions reprises dans « Prochaine action unique ». Les rapports sont hors dépôt, dans les dossiers `proof-<sha>/`. Restent ouverts : le troisième amendement de l’ADR énonce la manière dont la lecture a été faite sans l’attribuer à l’opérateur — l’ADR est append-only, il n’est pas édité, et le document de périmètre nomme cette phrase comme une déclaration ; un nom nu glissé dans la prose échappe à tout scan, seule l’épingle du document force sa relecture ; les consommateurs des 25 actifs antérieurs ne sont épinglés qu’en union. Aucun reçu `PROOF_ADEQUATE`, aucun `ALLOW`, aucune fusion, aucune activation ni suppression d’index.

`PROOF_WEAK` a une cause structurelle hors de ce dépôt : le classifieur de la politique de preuve active ne reconnaît ni `src/latent_compass/lab/`, ni `examples/`, ni `evidence/`, ni `docs/active-diagnosis-scope.md`. Un changement qui ne touche aucun test ne déclencherait aucun contrôle : un lot limité à la preuve scellée et au document de périmètre n’exigerait aucun reçu. Le corriger est une révision de politique, à mener par sa procédure N-1 dans un worktree isolé, jamais depuis ce candidat.

## Prochaine action unique

Le lanceur de la répétition générale est livré (`examples/lab_rehearsal_launcher.py`, mode d’emploi dans `docs/host-observations.md`) et son dernier relecteur l’a déclaré sûr à lancer une fois la clé créée, sous conditions. **Avant tout lancement réel, le propriétaire doit trancher où l’agent tourne** : sous Windows natif aucun bac à sable n’est actif — l’agent y lit et y écrit tout ce que peut le compte, dossier Codex habituel compris, et deux des six tâches lui demandent d’éditer et d’exécuter ; sous Linux (WSL) le bac à sable documenté borne ses écritures à sa copie, mais le CLI n’y est pas installé. Ne tranche pas à sa place et n’active pas à l’aveugle le bac à sable Windows du CLI : sa variante sans élévation pose des ACE de refus sur le système de fichiers, et elle n’a pas pu être exercée. Lance ensuite la répétition **seulement** quand la clé dédiée du propriétaire existe, avec `examples/lab-rehearsal-tasks.json`, une copie de ce dépôt public — jamais du second pilote, qui est privé —, `--cli-version` égal à la version que `codex --version` annonce (`0.116.0` à ce jour), le modèle que le propriétaire utilise d’habitude passé par `--model`, un dossier de sortie hors dépôt, et **`--max-sessions 1` d’abord : lis le rapport avant d’en lancer d’autres**. Attends-toi à un code de sortie `4`, pas `0`, si ce premier contact s’arrête à sa première session : c’est l’issue la plus probable avec un CLI jamais exercé, et ce n’est pas une panne. Vérifie avant de démarrer que la limite bloquante est active chez le fournisseur. Six sessions de quinze minutes au plus en tout, durée et jetons seulement — jamais une issue, sinon c’est un regard sur les résultats avant le gel. Le fichier de clé doit vivre dans un dossier qui ne contient rien d’autre auquel le propriétaire tienne : le lanceur y écrit son registre, son verrou et la copie jetable de la clé. **La clé doit être révoquée dès la répétition finie** : l’agent peut lire la clé sous laquelle il tourne. L’emplacement du fichier de clé est dans Linear (HOK-804), pas ici ; ne lis jamais son contenu à l’écran, ne l’écris dans aucun journal, et ne copie jamais les identifiants Codex habituels du propriétaire : un jeton renouvelé dans une copie peut déconnecter sa session active. La limite de 25 USD est une limite bloquante de projet chez le fournisseur ; son application n’est pas instantanée, garde donc ta propre limite de six sessions. Les 18 lignes `OPEN` restantes attendent pour la plupart les chiffres de cette répétition (bornes de coût et de latence, plafond, nombre de paires, seuils, règle du second temps) ; présente-les ensuite par paquets. Présente une décision à la fois, en langage simple et sans identifiants, avec ce que chaque option coûte. N’en remplis aucune de ton chef : tu consignes ce que le propriétaire décide, avec sa date, tu dis quand l’option venait de toi, et tu épingles l’enregistrement dans le test du même diff. Une fois toutes les lignes décidées, suis la procédure de gel du document — protocoles admis et scellés, ancre d’antériorité posée **avant** le premier essai. Le travail logiciel qui ne dépendait d’aucune décision est livré. Les critères HOK-803 encore ouverts — séparation effective des stores, parité des deux hôtes réels, adaptateurs vérifiés sur des versions d’hôtes identifiées — demandent de vraies sessions Codex et Claude Code : cadre leur coût et leur isolation avec le propriétaire avant de les lancer.

HOK-805/806 restent conditionnés par leurs preuves et autorisations propres. La correction des preuves formelles fait partie de la livraison ; elle ne remplace pas les capacités fonctionnelles manquantes.

## Décisions du propriétaire en attente

- ~~**U1**~~ — tranchée le 2026-09-19 : oui, un second dépôt personnel rejoint le pilote (révision d’inventaire `v1.2.0`, troisième amendement de l’ADR 0011). CCC a désormais un sujet ; un seul dépôt reste un seul sujet, dont rien ne s’extrapole.
- **Politique de preuve** — ajouter au classifieur une règle couvrant le noyau de décision, les preuves scellées et le document de périmètre de ce dépôt.
- **HOK-804** — population, budgets, métrique primaire, marge de non-infériorité et amélioration de coût utile, figés avant la première mesure (U8). Les 34 décisions sont listées dans `docs/active-diagnosis-preregistration.md` : 16 sont prises (2026-09-19, en une séance, chacune sur proposition de l’agent), 18 sont `OPEN`. La règle de tirage des tâches reste à écrire et à faire approuver avant le gel. Le protocole doit aussi dire sur quels snapshots du second pilote on mesure : à la lecture de l’opérateur, la date de fichier la plus récente parmi ses artefacts d’index et les caches de routage des deux hôtes précédait la lecture d’environ 23 jours. Une date de fichier ne dit rien de la fraîcheur ni de l’usage, et ce chiffre, comme toutes les tailles et tous les comptes du document de périmètre, est une déclaration de l’opérateur que ce dépôt ne peut pas montrer.
- **Historique Git** — le commit `4abf502` de cette branche contient encore un chemin privé de l’opérateur ; le retirer exige une réécriture d’historique et un push forcé.

## Contraintes

- Linear personnel uniquement : workspace/team Hoklims/HOK, compte `hoklims@gmail.com`. Projet réutilisé `2d29f7e2-f682-4f08-a582-db96a5ed31e3`, « Latent Compass — Diagnostic actif et routage gouverné » ; milestone `1600ae66-8f78-4833-9c81-81914858f5ce`. Aucun doublon ni réactivation des anciennes issues annulées.
- Garde les contrats, stores et CLI historiques compatibles ; les nouvelles capacités restent isolées. Un seul auteur par checkout. Ne modifie pas les anciens worktrees `E:\latent-compass`, `E:\latent-compass-hok800`, `E:\latent-compass-hok803` ou `E:\latent-compass-merged` pour reprendre ce lot.
- Les coûts, mondes et probabilités sont des hypothèses du modèle ; aucune optimalité universelle, efficacité réelle ou authenticité ne découle d’un seal. Une lecture vide ne prouve pas une absence hors de sa portée. Aucun résultat empirique inventé ni ancien holdout réutilisé.
- Une preuve committée ne s’édite jamais sur place : une correction est une révision ajoutée à côté, et un test épingle les seals de chaque révision.
- Le conseiller ne gagne aucune autorité d’exécution, de promotion ou de modification du juge Semctx. Stores Codex/Claude et identités personnelles/professionnelles restent séparés. Prépare les intégrations dans un banc isolé ; ne change pas les hooks/configurations actifs pour démontrer un succès.
- Ne contourne pas `proof-integrity-review` : garde la politique active intacte, fournis les témoins nécessaires, utilise un réviseur neuf et réellement restreint, puis le gate exigé avant toute conclusion de readiness. Une consigne « lecture seule » à un agent disposant d’outils d’écriture n’est pas une restriction effective. Compare les empreintes d’un epoch committé aux objets Git, pas aux octets CRLF du worktree : `git archive` convertit les fins de ligne sauf avec `-c core.autocrlf=false`.
- HOK-804, HOK-247 avec ACTIVATE applicable et HOK-740 précèdent le pilote HOK-805. Ne suppose pas livrés HOK-737/740 ou HOK-501. L’autorisation du pilote et celle d’une destruction irréversible restent distinctes. Ne transforme pas cette reprise en autorisation de merge, d’activation ou de purge.
- Conserve des états Linear fidèles aux preuves. Le parent et HOK-799 à HOK-803 sont In Progress ; HOK-804 à HOK-806 sont Backlog. Mets à jour les capacités livrées, les restes et la PR, puis relis les objets modifiés. N’étiquette pas tout Done sur la seule CI.

## Vérification

Utilise Python 3.13 et les dépendances uv figées. Tests ciblés pendant les corrections ; avant publication du candidat, exécute les gates du dépôt :

    uv run --frozen ruff format --check .
    uv run --frozen ruff check .
    uv run --frozen mypy
    uv run --frozen pytest -o addopts='' -q
    uv build --no-sources

Démos : `uv run --frozen python examples/lab_source_demo.py` et `uv run --frozen python examples/lab_host_bench.py`. Revalide le wheel hors checkout et la CI des deux OS au SHA final. Sous Windows, n’exécute pas de shim POSIX sans extension et ne confonds pas import/type-check Linux avec une exécution Linux.

Commence par vérifier branche, HEAD et arbre de travail, lis les références, puis poursuis la prochaine tranche vérifiable sans demander une nouvelle permission pour les travaux locaux déjà autorisés.
